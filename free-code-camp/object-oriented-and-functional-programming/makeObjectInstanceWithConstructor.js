var Car = function() {
  this.wheels = 4;
  this.engines = 1;
  this.seats = 5;
};

// Make a new Car instance and add a property called nickname with assignment of 
// the string "myCar" as it's value.

var myCar = new Car();
myCar.nickname = "myCar";