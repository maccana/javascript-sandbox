var Car = function(wheels, seats, engines) {
  this.wheels = wheels;
  this.seats = seats;
  this.engines = engines;
};

// Create new Car instance and pass params for wheels, seats and engines
var myCar = new Car(4, 6, 1);